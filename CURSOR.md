# Правила для Cursor AI (Color Block Jam)

> Цей файл містить інструкції для AI асистента при роботі з проєктом

---

## Правило #0 - НАЙВАЖЛИВІШЕ

**Спілкуватись українською мовою.**

Код, коментарі та git commits - англійською.

---

## Правило #1 - Читання інструкцій перед роботою

**ОБОВ'ЯЗКОВИЙ порядок дій при кожному повідомленні:**

1. **СПОЧАТКУ:** Прочитати CURSOR.md (цей файл)
2. **ПОТІМ:** Ознайомитись з документацією в `docs/`
3. **ТІЛЬКИ ПІСЛЯ ЦЬОГО:** Написати "Я прочитав правило номер 1"
4. **І ТІЛЬКИ ТОДІ:** Починати відповідь на запит користувача

**Чому це важливо:**
- Це гарантує що ти завжди пам'ятаєш всі правила
- Особливо КРИТИЧНЕ правило: **НІКОЛИ не робити `git push` без дозволу**
- Це "sanity check" що ти готовий працювати правильно

**Якщо ти не напишеш "Я прочитав правило номер 1" → це сигнал що ти пропустив інструкції!**

---

## Правило #1.5 - ЧИТАННЯ ДОКУМЕНТАЦІЇ ПЕРЕД РОЗРОБКОЮ

**КРИТИЧНО ВАЖЛИВО:**

**Перед початком реалізації БУДЬ-ЯКОГО feature:**

1. **ЗНАЙТИ відповідну документацію:**
   - `docs/GAME_DESIGN.md` - геймплей, механіки, економіка, монетизація
   - `docs/UI_UX_DESIGN.md` - екрани, компоненти, анімації, стиль
   - `docs/TECHNICAL_SPEC.md` - архітектура, моделі, алгоритми
   - `docs/GAME_ANALYSIS.md` - аналіз оригінальної гри
   - `docs/ROADMAP.md` - статус розробки, фази

2. **ПРОЧИТАТИ ПОВНІСТЮ документ:**
   - НЕ шукай тільки конкретну секцію
   - НЕ припускай що знаєш як має бути реалізовано
   - НЕ економ токени на читанні документації
   - Читай ВСЮ специфікацію: UI layout, розміри, кольори, позиції, поведінку

3. **ПЕРЕВІРИТИ розуміння:**
   - Переконайся що знаєш ТОЧНІ специфікації (пікселі, padding, alignment)
   - Знаєш всі компоненти що потрібно реалізувати
   - Розумієш послідовність екранів та взаємодію

4. **ТІЛЬКИ ПОТІМ починати кодити**

**Чому це КРИТИЧНО:**
- ❌ Без читання документації → неправильний layout → переробка → витрата часу
- ❌ Економія токенів на читанні → дорожча помилка в реалізації
- ✅ Повне читання документу → правильна реалізація з першого разу
- ✅ Користувач отримує те що задокументовано, не твою інтерпретацію

**Приклад НЕПРАВИЛЬНОГО підходу:**
```
Користувач: "Реалізуй Win Dialog"
Асистент: "Створюю Win Dialog з моїм розумінням..."
Результат: Кнопки не там, кольори не ті, layout неправильний
```

**Приклад ПРАВИЛЬНОГО підходу:**
```
Користувач: "Реалізуй Win Dialog"
Асистент: "Спочатку читаю docs/GAME_DESIGN.md та docs/UI_UX_DESIGN.md..."
Асистент: "Бачу специфікацію: stars animation, coins earned, Next/Replay buttons..."
Асистент: "Реалізую згідно документації..."
Результат: Все правильно з першого разу
```

**ЗАБОРОНЕНО:**
- ❌ Починати кодити без читання документації
- ❌ Читати тільки частину документу (треба ВСЮ!)
- ❌ Економити токени на читанні специфікацій
- ❌ Припускати "я знаю як це має бути"
- ❌ Імпровізувати розміри, кольори, позиції без документації

---

## Правило #2 - ДОКУМЕНТУВАННЯ

**КРИТИЧНО: Правила написання та оновлення документації**

### Порядок роботи з документацією:

1. **СПОЧАТКУ код, ПОТІМ документація:**
   - ✅ **ЗАВЖДИ** читати всі релевантні файли коду
   - ✅ **ЗАВЖДИ** перевіряти реальну реалізацію
   - ✅ **ТІЛЬКИ ПОТІМ** писати в документ
   - ❌ **НІКОЛИ** не писати документацію базуючись на припущеннях

2. **Перевірка фактів:**
   - ✅ Використовувати Read/Grep/Glob для перевірки коду
   - ✅ Шукати конкретні методи, параметри, обмеження в коді
   - ✅ Перевіряти constants, defaults, conditions
   - ❌ НЕ припускати "зазвичай так роблять в іграх"
   - ❌ НЕ писати обмеження якщо не бачив їх в коді

3. **Якщо не впевнений:**
   - ✅ **ЗАПИТАТИ користувача** перед написанням
   - ✅ Додати коментар "TODO: verify implementation" в документ
   - ✅ Явно сказати "я припускаю що..." (не писати як факт)
   - ❌ **НІКОЛИ** не вигадувати деталі
   - ❌ **НІКОЛИ** не писати припущення як підтверджені факти

### Чому це КРИТИЧНО:

**Наслідки неправильної документації:**
- ❌ Агенти читають документацію → базуються на ній → дають неправильні рекомендації
- ❌ "Функція X обмежена" (в документі) → насправді "необмежена" (в коді) → агент каже "треба покращити"
- ❌ Витрата часу користувача на виправлення помилок в документації
- ❌ Втрата довіри до документації та до асистента

**Переваги правильної документації:**
- ✅ Документація = джерело правди про реалізацію
- ✅ Агенти дають правильні рекомендації
- ✅ Користувач економить час
- ✅ Можна довіряти документації

### Перевірочний чеклист перед записом в документ:

- [ ] Я прочитав код який документую?
- [ ] Я перевірив всі параметри/обмеження/умови?
- [ ] Я впевнений на 100% що це відповідає реальності?
- [ ] Якщо НІ → я запитав користувача або додав "TODO: verify"?

**Якщо хоча б один чекбокс НЕ виконано → НЕ писати в документ!**

---

## Правило #3 - REFERENCE GAME

**Наша референсна гра:**
- **Color Block Jam** by Rollic/Gybe Games

**Аналіз оригіналу:**
- `docs/GAME_ANALYSIS.md` - реверс-інжиніринг, бінарна структура
- `res/ColorBlockJam_Analysis/` - інструменти парсингу рівнів
- `res/ColorBlockJam_Analysis/level_visualizer.html` - візуалізатор рівнів

**Рівні:**
- 27 рівнів експортовано в `assets/levels/`
- 1557 рівнів в оригіналі

**Будь-яке порівняння базується на оригінальній грі:**
- Механіки (moveDirection, iceCount, innerBlockType)
- UI/UX дизайн
- Монетизація та економіка
- Візуальний стиль

---

## Правило #4 - GIT

**КРИТИЧНО:**

- ❌ **НІКОЛИ** не робити `git push` без явного дозволу користувача
- ✅ Завжди питати: "Зберегти та запушити зміни?"
- ✅ Коміти англійською з conventional commits:
  - `feat:` - нова функціональність
  - `fix:` - виправлення багів
  - `docs:` - документація
  - `test:` - тести
  - `refactor:` - рефакторинг

---

## Правило #5 - КОЛЬОРИ ТА КОНСТАНТИ

**Використовувати централізовані константи:**

```dart
// lib/core/constants/colors.dart
AppColors.buttonGreen           // Кнопки Play, Next
AppColors.buttonRed             // Close, Cancel
AppColors.gold                  // Stars, coins
AppColors.dialogGradientLight   // Діалоги (top)
AppColors.dialogGradientDark    // Діалоги (bottom)
AppColors.levelCompleted        // Green node
AppColors.levelLocked           // Gray node
```

**ЗАБОРОНЕНО:**
- ❌ Хардкодити кольори типу `Color(0xFF5BB8E8)`
- ✅ Додавати нові кольори в `AppColors`

---

## Правило #6 - ТЕСТУВАННЯ

**Використовувати test helpers:**

```dart
// test/helpers/test_helpers.dart
TestHelpers.wrapWidget(widget)     // Обгортка для віджетів
TestHelpers.wrapScreen(widget)     // Обгортка для екранів
TestHelpers.initServices()         // Ініціалізація моків
tester.setLargeScreenSize()        // Великий екран (800x1200)
tester.tapAndSettle(finder)        // Tap + pumpAndSettle
```

**Правила анімацій:**
- ✅ Використовувати `Timer` замість `Future.delayed`
- ✅ Зберігати таймери в список для cleanup
- ✅ Cancel всі таймери в `dispose()`
- ✅ Перевіряти `mounted` перед викликом setState/forward

**Приклад:**
```dart
final List<Timer> _timers = [];

void startAnimation() {
  final timer = Timer(Duration(milliseconds: 200), () {
    if (mounted) controller.forward();
  });
  _timers.add(timer);
}

@override
void dispose() {
  for (final timer in _timers) timer.cancel();
  super.dispose();
}
```

---

## Правило #7 - СТРУКТУРА ПРОЄКТУ

```
lib/
├── core/
│   ├── constants/     # AppColors, AppConstants
│   ├── models/        # GameBlock, GameDoor, GameLevel
│   ├── services/      # StorageService, AudioService
│   └── widgets/       # Shared widgets
├── features/
│   ├── game/          # GameScreen, widgets/
│   ├── level_map/     # LevelMapScreen, widgets/
│   ├── settings/      # SettingsScreen
│   ├── shop/          # ShopScreen
│   ├── profile/       # ProfileScreen
│   ├── splash/        # SplashScreen
│   └── ...
└── main.dart

docs/                  # Документація
test/                  # Тести
  ├── helpers/         # Test helpers
  └── features/        # Feature tests
assets/levels/         # JSON рівні
res/ColorBlockJam_Analysis/  # Аналіз оригіналу
```

---

## Правило #8 - СПОЧАТКУ НАША ДОКУМЕНТАЦІЯ, ПОТІМ ІНТЕРНЕТ

**КРИТИЧНО: При виникненні помилок або потребі в технічній інформації**

### Порядок пошуку інформації:

1. **СПОЧАТКУ шукати в нашій документації:**
   - `docs/` - технічні документи
   - `docs/README.md` - індекс документів
   - Grep/Glob по проєкту для пошуку релевантних файлів

2. **ТІЛЬКИ ЯКЩО в нашій документації НЕМАЄ** → йти в інтернет (WebSearch/WebFetch)

### Конкретні документи:

| Тема | Наш документ |
|------|--------------|
| Геймплей | `docs/GAME_DESIGN.md` |
| UI/UX | `docs/UI_UX_DESIGN.md` |
| Архітектура | `docs/TECHNICAL_SPEC.md` |
| Оригінальна гра | `docs/GAME_ANALYSIS.md` |
| Статус розробки | `docs/ROADMAP.md` |

### ЗАБОРОНЕНО:

- ❌ Йти в WebSearch/WebFetch БЕЗ перевірки нашої документації
- ❌ Припускати "в нас немає такої документації"
- ❌ Шукати в інтернеті те що вже є в `docs/`

### Перевірочний чеклист перед WebSearch:

- [ ] Я перевірив `docs/README.md`?
- [ ] Я зробив Grep по ключових словах в проєкті?
- [ ] Я перевірив папку `docs/`?
- [ ] Я ТОЧНО впевнений що в нас немає цієї інформації?

**Якщо хоча б один чекбокс НЕ виконано → НЕ йти в інтернет!**

---

## Правило #9 - ПРАВИЛА ДЛЯ АГЕНТІВ

### Для QA агента (/mobile-game-qa-review)

**ОБОВ'ЯЗКОВИЙ порядок дій:**
1. Прочитати `docs/GAME_DESIGN.md`
2. Прочитати `docs/TECHNICAL_SPEC.md`
3. Перевірити код на відповідність документації
4. Дати фідбек з пріоритетами (Critical → Low)

### Для UX агента (/mobile-game-ux-review)

**ОБОВ'ЯЗКОВИЙ порядок дій:**
1. Прочитати `docs/UI_UX_DESIGN.md`
2. Прочитати `docs/GAME_DESIGN.md`
3. Порівняти з оригінальною грою (screenshots)
4. Дати рекомендації

### Для Architecture агента (/sdk-architecture-review)

**ОБОВ'ЯЗКОВИЙ порядок дій:**
1. Прочитати `docs/TECHNICAL_SPEC.md`
2. Перевірити структуру коду
3. Виправити проблеми по пріоритетах

---

## Правило #10 - ПОТОЧНИЙ СТАТУС

**Завершені фази:**
- ✅ Phase 0: Foundation (Core gameplay)
- ✅ Phase 1: Polish & UX (Audio, settings)
- ✅ Phase 1.5: Core Systems (Timer, Lives)
- ✅ Phase 2: Core Features (Coins, Boosters)
- ✅ Phase 3: Screens (Splash, Map, Shop)
- ✅ Phase 4: Polish (Profile, Tests)

**Наступні:**
- ⏳ Phase 5: Deployment (Branding, Builds)
- 📅 Phase 6: Monetization (Ads, IAP)
- 📅 Phase 7: Content (More levels)

**MVP Progress: 95%**

---

## Правило #11 - FLUTTER/DART

**Команди:**
```bash
# Запуск
fvm flutter run -d windows
fvm flutter run -d chrome

# Тести
fvm flutter test

# Конкретний тест
fvm flutter test test/features/game/widgets/win_dialog_test.dart

# Парсинг рівнів
python res/ColorBlockJam_Analysis/tools/parse_from_unity.py
python res/ColorBlockJam_Analysis/tools/export_game_levels.py
```

**Tech Stack:**
- Flutter 3.x з FVM
- Dart 3.x
- shared_preferences для storage
- CustomPainter для рендерингу гри

---

## Фінальний чеклист перед відповіддю

- [ ] Прочитав CURSOR.md?
- [ ] Прочитав релевантну документацію з `docs/`?
- [ ] Написав "Я прочитав правило номер 1"?
- [ ] Перевірив існуючий код перед змінами?
- [ ] Використовую AppColors замість хардкоду?
- [ ] Тести мають proper cleanup (timers, controllers)?
- [ ] Не пушу без дозволу?

---

*Останнє оновлення: 2025-12-24*
